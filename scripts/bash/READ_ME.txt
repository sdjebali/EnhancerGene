# bash scripts:
# - 'restrictionsfragments.sh': generate the restrictions fragments for an
#enzyme. I used it for HindIII.
# - 'separate_Venn.sh':  from the connected component files, separate them into
#new files and create a consensus interval for each connected component.
# - 'coverage_connectedcomponent_chr22.sh': create and use file to see the
#coverage of the connected component.
